import { useAppState } from "@/lib/contexts/AppStateContext";

// Simple re-export or alias if needed, but context already exports a hook.
// The plan mentions `hooks/useAppState.ts` separate from context.
// Better to just re-export or move hook logic here if preferred.
// For now, simply re-exporting to match file structure plan.

export { useAppState };
